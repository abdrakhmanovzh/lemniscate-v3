import Link from 'next/link';
import { useSession } from 'next-auth/react';
import { Loading, createUsername } from '@/shared';
import { useFetchUsers } from '../hooks';

export const UsersBar = () => {
  return <></>;
};
